#pragma once

// Compatibility shim for Arduino_GFX when building against
// framework-arduinoespressif32 2.0.x, where this header is not shipped.
// The current project does not use any symbols from this header.
