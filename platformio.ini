; This repository now targets Raspberry Pi Zero W instead of PlatformIO/ESP32.
; Run `python src/main.py` after installing `requirements.txt`.
; See README.md for Pi deployment and optional access-point setup.
